'use strict';

describe('Filter: file pool filter tests', function () {

    //var fakePool = function (v) {
    //    return {
    //        'id': v,
    //        'label':'File Pool ' + v,
    //        'totalCapacity': 3000,
    //        'freeCapacity': 1000,
    //        'usedCapacity': 2000,
    //        'healthy': true,
    //        'chunkSize': 100,
    //        'tiered': true
    //    };
    //};

    beforeEach(module('rainierApp'));

    //it('has a file-pool-search filter', inject(function ($filter) {
    //    expect($filter('file-pool-search')).not.toBeNull();
    //}));

    //var i=0;
    //xit('should return true if the input is not equal to \'f\' / 0 / \'0\' / \'false\' / \'no\'/ \'n\' or an empty array ', inject(function (filePoolSearch) {
    //    var fakePoolObject = fakePool(i);
    //    i++;
    //
    //    expect(filePoolSearch(fakePoolObject));
    //}));

});
